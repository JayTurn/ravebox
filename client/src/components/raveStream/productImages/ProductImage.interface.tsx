/**
 * ProductImage.interface.tsx
 * Interfaces for the product image.
 */

// Interfaces.
import { ImageAndTitle } from '../../elements/image/Image.interface';

/**
 * Properties for the product image.
 */
export interface ProductImageProps {
  image: ImageAndTitle;
}
