/**
 * ProductDetailsForm.tsx
 * Product details form component.
 */

// Modules.
import * as React from 'react';

// Interfaces.
import { ProductDetailsFormProps } from './ProductDetailsForm.interface';

/**
 * Renders the product details form component.
 */
const ProductDetailsForm: React.FC<ProductDetailsFormProps> = (props: ProductDetailsFormProps) => {
  return (
    <div>
      Product Details form
    </div>
  );
};

export default ProductDetailsForm;
