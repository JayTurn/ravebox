/**
 * SponsoredChip.interface.tsx
 * Interfaces for the sponsored content chip.
 */

// Modules.
import * as React from 'react';

/**
 * Interfaces for the sponsored content chip component.
 */
export interface SponsoredChipProps { }
