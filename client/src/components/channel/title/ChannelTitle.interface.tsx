/**
 * ChannelTitle.interface.tsx
 * Interfaces for the styled user details displayed as the channel title.
 */

export interface ChannelTitleProps {
  avatar?: string;
  handle: string;
  ravesCount: string;
}
