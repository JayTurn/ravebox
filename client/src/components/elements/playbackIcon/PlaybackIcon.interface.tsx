/**
 * PlaybackIcon.interface.tsx
 * Interface for the playback icon.
 */

/**
 * Playback icon properties.
 */
export interface PlaybackIconProps {
  loading: boolean;
  playing: boolean;
  unplayed: boolean;
}
