/**
 * ViewState.enum.tsx
 * Enumerators for the view state.
 */

/**
 * View state for simplified rendering.
 */
export enum ViewState {
  FOUND = 'found',
  NOT_FOUND = 'not-found',
  WAITING = 'waiting'
}
