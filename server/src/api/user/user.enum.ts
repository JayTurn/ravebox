/**
 * user.enum.ts
 * Enumerators for the user.
 */

// User roles.
export enum UserRole {
  USER = 'user',
  ADMIN = 'admin'
}

// User types.
export enum UserType {
  ANONYMOUS = 'anonymous',
  AUTHENTICATED = 'authenticated'
}
